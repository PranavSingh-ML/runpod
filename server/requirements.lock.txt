# PROVISIONAL LOCK - replace this whole file with phase0/requirements.lock.txt after Phase 0
# (pip freeze taken inside the v0 image on the GPU). Until then the Docker build installs
# exactly these top-level pins (verified on PyPI 2026-09-17) and lets pip resolve the rest.
# torch/torchvision come from the Dockerfile torch layer and must not be listed here.
diffusers==0.40.0
transformers==5.17.0
accelerate==1.15.0
safetensors>=0.5
peft>=0.17
sentencepiece
protobuf
huggingface_hub[hf_xet]
bitsandbytes==0.50.2
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart
pillow
